---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import SectionCard from "../components/sectionCard.astro";
import HeadersSection from "../components/headersSection.astro";
import { Image } from "astro:assets";
import Mission from "../assets/mision.svg";
import Vision from "../assets/vision.svg";
import Cards from "../components/cards.astro";
import teamCardsContent from "../data/teamCardsContent";
import TeamCards from "../components/TeamCards.astro";
import WhatsCard from "../components/whatsCard.astro";
---

<Layout>
  <Navbar />

  <!-- Hero Section -->
  <section class="p-5 opacity-0 animate-fade-in">
    <div class="max-w-7xl mx-auto">
      <SectionCard
        HeaderText="Descripción general"
        title="Bienvenidos al Colegio de Educación por Ciclos de Oriente"
        description="ofrecemos una formación integral y flexible que se adapta a las necesidades de cada etapa escolar, permitiendo que los estudiantes avancen a su propio ritmo y fortalezcan sus competencias académicas, sociales y emocionales. Contamos con un equipo docente comprometido, estrategias pedagógicas actualizadas y un ambiente seguro e inclusivo que promueve valores como el respeto, la responsabilidad y la colaboración. "
      />
    </div>
  </section>

  <!-- Mission & Vision Section -->
  <section id="mision" class="p-5 flex flex-col gap-5 scroll-reveal">
    <div class="max-w-7xl mx-auto w-full">
      <HeadersSection text="Mision & Vision" />
      <h2 class="text-center font-bold text-3xl lg:text-4xl xl:text-5xl mt-5">
        Nuestra Misión y Visión
      </h2>
      <p
        class="text-center font-medium text-[#333333] text-lg lg:text-lg xl:text-xl mt-3 lg:w-[80%] xl:w-[70%] mx-auto"
      >
        Formamos personas integrales y autónomas, guiadas por valores y un
        modelo educativo flexible que impulsa su crecimiento y su capacidad de
        transformar su entorno.
      </p>

      <!-- Mission & Vision container -->
      <div
        class="flex flex-col gap-10 mt-10 lg:flex-row lg:justify-center lg:items-stretch lg:gap-8 xl:gap-10"
      >
        <!-- Mission Card -->
        <div
          class="border-2 border-black rounded-xl p-7 flex flex-col gap-7 lg:flex-1 lg:max-w-[600px] hover:shadow-2xl hover:scale-105 transition-all duration-300 scroll-reveal"
        >
          <!-- Header Container -->
          <div class="w-full flex justify-between items-center">
            <h3 class="text-3xl font-bold lg:text-4xl">Misión</h3>
            <figure class="flex-shrink-0 animate-pulse">
              <Image
                src={Mission}
                alt={"mision de ceco"}
                class="w-12 h-12 lg:w-14 lg:h-14 xl:w-16 xl:h-16"
              />
            </figure>
          </div>
          <p
            class="text-[#333333] font-medium text-base lg:text-lg xl:text-xl leading-relaxed"
          >
            Guiándonos por nuestra filosofía y nuestros valores, EL COLEGIO DE
            EDUCACIÓN POR CICLOS DE ORIENTE-CECO, asume la labor educativa para
            contribuir en el desarrollo de sus educandos como seres humanos, con
            principios Cristianos, de libertad, de orden y de justicia;
            atendiendo las modernas teorías pedagógicas, en procura de la
            formación de un ser crítico, autónomo, reflexivo e investigador, que
            sea capaz de comprender y construir desde la ciencia básicas,
            humanas y sociales, las artes, la tecnología y la innovación, las
            mejores condiciones de vida para sí mismo y los demás.
          </p>
        </div>

        <!-- Vision Card -->
        <div
          class="border-2 border-black rounded-xl p-7 flex flex-col gap-7 lg:flex-1 lg:max-w-[600px] hover:shadow-2xl hover:scale-105 transition-all duration-300 scroll-reveal"
          style="animation-delay: 200ms"
        >
          <!-- Header Container -->
          <div class="w-full flex justify-between items-center">
            <h3 class="text-3xl font-bold lg:text-4xl">Visión</h3>
            <figure class="shrink-0 animate-pulse">
              <Image
                src={Vision}
                alt={"vision de ceco"}
                class="w-12 h-12 lg:w-14 lg:h-14 xl:w-16 xl:h-16"
              />
            </figure>
          </div>
          <p
            class="text-[#333333] font-medium text-base lg:text-lg xl:text-xl leading-relaxed"
          >
            Para el 2032 nos visionamos como una Institución líder en nuestra
            área de influencia, con un sistema de educación por ciclos para
            adultos y jóvenes en extra edad, con calidad. De igual manera nos
            vemos como generadores de agentes transformadores de cambios, que
            ayuden a dignificar al ser humano y a la sociedad.
          </p>
        </div>
      </div>
    </div>
  </section>

  <Footer />
  <WhatsCard />
</Layout>

<script>
  // Intersection Observer para animaciones al hacer scroll
  const observerOptions = {
    threshold: 0.1,
    rootMargin: "0px 0px -50px 0px",
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("is-visible");
      }
    });
  }, observerOptions);

  // Observar todos los elementos con la clase scroll-reveal
  document.querySelectorAll(".scroll-reveal").forEach((el) => {
    observer.observe(el);
  });
</script>

<style>
  /* Animación de fade-in para el hero */
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 1s ease-out forwards;
  }

  /* Animaciones de scroll reveal */
  .scroll-reveal {
    opacity: 0;
    transform: translateY(50px);
    transition:
      opacity 0.8s ease-out,
      transform 0.8s ease-out;
  }

  .scroll-reveal.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Smooth scroll para toda la página */
  html {
    scroll-behavior: smooth;
  }

  /* Animación de pulse suave para iconos */
  @keyframes pulse {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.8;
    }
  }

  .animate-pulse {
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }
</style>
