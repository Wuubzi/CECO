---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import SectionCard from "../components/sectionCard.astro";
import HeadersSection from "../components/headersSection.astro";
import Footer from "../components/Footer.astro";
import DiferentsCardsContent from "../data/diferentsCardsContent";
import Cards from "../components/cards.astro";
import MateriaCards from "../components/materiaCards.astro";
import materiasCardsContent from "../data/materiasCardsContent";
import galleryCardsContent from "../data/galleryCardsContent";
import GaleryCards from "../components/galeryCards.astro";
import WhatsCard from "../components/whatsCard.astro";
---

<Layout>
  <Navbar />

  <!-- Hero Section -->
  <section class="p-5 opacity-0 animate-fade-in">
    <div class="max-w-7xl mx-auto">
      <SectionCard
        HeaderText="Academia"
        title="Lo que Nuestros Estudiantes Aprenden"
        description="En nuestros ciclos educativos, los estudiantes desarrollan las competencias esenciales para culminar su bachillerato, fortaleciendo habilidades académicas, personales y ciudadanas que les permiten avanzar con seguridad hacia nuevas oportunidades laborales o educativas."
      />
    </div>
  </section>

  <!-- Diferencias Section -->
  <section id="diferente" class="p-5 flex flex-col gap-5 scroll-reveal">
    <div class="max-w-7xl mx-auto w-full">
      <HeadersSection text="Nuestros pilares" />
      <h2 class="font-bold text-3xl lg:text-4xl xl:text-5xl text-center mt-5">
        Lo que nos hace diferente
      </h2>
      <p
        class="font-medium text-lg lg:text-lg xl:text-xl text-center text-[#333333] mt-3 lg:w-[80%] xl:w-[70%] mx-auto"
      >
        En nuestro colegio de validación, ofrecemos una experiencia académica
        flexible, acompañada y enfocada en garantizar que cada estudiante
        complete su bachillerato con éxito, sin importar su edad o situación.
      </p>

      <!-- Cards Container -->
      <div
        class="flex flex-col gap-10 pt-10 lg:grid lg:grid-cols-2 xl:grid-cols-3 lg:gap-8 xl:gap-10"
      >
        {
          DiferentsCardsContent.map((card, index) => (
            <div
              class="scroll-reveal"
              style={`animation-delay: ${index * 150}ms`}
            >
              <Cards
                title={card.title}
                description={card.description}
                icon={card.icon}
              />
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Contenidos Section -->
  <section id="contenido" class="p-5 flex flex-col gap-5 scroll-reveal">
    <div class="max-w-7xl mx-auto w-full">
      <HeadersSection text="Contenido" />
      <h2 class="font-bold text-3xl lg:text-4xl xl:text-5xl text-center mt-5">
        Contenidos Clave para la Validación
      </h2>
      <p
        class="font-medium text-lg lg:text-lg xl:text-xl text-center text-[#333333] mt-3 lg:w-[85%] xl:w-[75%] mx-auto"
      >
        En nuestra institución de Validación de Bachillerato, nos enfocamos en
        proporcionar el conocimiento esencial que los jóvenes y adultos
        necesitan para superar los exámenes de validación con éxito. Nuestros
        programas académicos cubren las áreas fundamentales, permitiendo a los
        estudiantes construir una base sólida y acelerar su proceso de
        graduación.
      </p>

      <!-- Materias Container -->
      <div
        class="flex flex-col gap-8 mt-10 lg:grid lg:grid-cols-2 xl:grid-cols-3 lg:gap-8"
      >
        {
          materiasCardsContent.map((card, index) => (
            <div
              class="scroll-reveal"
              style={`animation-delay: ${index * 100}ms`}
            >
              <MateriaCards
                title={card.title}
                description={card.description}
                image={card.image}
              />
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Gallery -->
  <section id="instalaciones" class="p-5 flex flex-col gap-5 scroll-reveal">
    <div class="max-w-7xl mx-auto w-full">
      <HeadersSection text="Galería" />
      <h2 class="font-bold text-3xl lg:text-4xl xl:text-5xl text-center mt-5">
        Nuestras Instalaciones y Experiencias
      </h2>
      <p
        class="font-medium text-lg lg:text-lg xl:text-xl text-[#333333] text-center mt-3 lg:w-[80%] xl:w-[70%] mx-auto"
      >
        Te invitamos a explorar nuestra galería e sumergirte en un recorrido
        visual de los espacios de aprendizaje, la tecnología y el ambiente
        colaborativo de nuestro programa de Validación de Bachillerato.
      </p>

      <!-- Gallery Container -->
      <div class="mt-10 scroll-reveal" style="animation-delay: 200ms">
        {
          galleryCardsContent.map((card) => (
            <GaleryCards
              images={card.images}
              title={card.title}
              description={card.description}
            />
          ))
        }
      </div>
    </div>
  </section>

  <Footer />
  <WhatsCard />
</Layout>

<script>
  // Intersection Observer para animaciones al hacer scroll
  const observerOptions = {
    threshold: 0.1,
    rootMargin: "0px 0px -50px 0px",
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("is-visible");
      }
    });
  }, observerOptions);

  // Observar todos los elementos con la clase scroll-reveal
  document.querySelectorAll(".scroll-reveal").forEach((el) => {
    observer.observe(el);
  });
</script>

<style>
  /* Animación de fade-in para el hero */
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 1s ease-out forwards;
  }

  /* Animaciones de scroll reveal */
  .scroll-reveal {
    opacity: 0;
    transform: translateY(50px);
    transition:
      opacity 0.8s ease-out,
      transform 0.8s ease-out;
  }

  .scroll-reveal.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Smooth scroll para toda la página */
  html {
    scroll-behavior: smooth;
  }
</style>
