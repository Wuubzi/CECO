---
const { title, description, button, color } = Astro.props;
---

<div
  class="border-2 border-[#262626] flex flex-col items-center justify-center rounded-2xl p-7 gap-5 w-full md:w-[400px] lg:w-[450px] hover:shadow-lg transition-shadow"
>
  <h2 class="font-bold text-2xl">{title}</h2>
  <p class="text-[#4C4C4D] text-center">{description}</p>
  <button
    class=`bg-[${color}] w-max rounded-xl h-max p-4 text-[#333333] font-medium`
    >{button}
  </button>
</div>
